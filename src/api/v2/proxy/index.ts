import root from './root.api'
import down from './down.api'
import all from './all.api'
import update from './update.api'
import config from './config.api'

export default {
  root: root,
  update: update,
  all: all,
  down: down,
  config: config
}
